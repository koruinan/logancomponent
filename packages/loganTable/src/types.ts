export interface TableOptions {
    prop: string,
    label: string,
    width?: string | number,
    align?: 'left' | 'center' | 'right',
    slot?: string,
    action?: boolean,
    editable?: boolean
} 